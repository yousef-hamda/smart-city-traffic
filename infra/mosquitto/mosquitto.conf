# Mosquitto dev broker — anonymous access is acceptable ONLY for local dev;
# production deploys terminate MQTT behind authenticated listeners (see infra/k8s).
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
